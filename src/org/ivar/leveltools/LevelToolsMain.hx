package org.ivar.leveltools;

import org.ivar.leveltools.TilemapIterator;
import org.ivar.leveltools.LevelTools;
import org.ivar.leveltools.DameLevel;
import org.ivar.leveltools.TiledLevel;

/**
 * @author migimunz
 * A dummy class that contains main. Used to make nme build the library as a standalone 
 * and generate xml for documentation. Shouldn't be used at all, might be removed in the future.
 */
class LevelToolsMain
{
	static public function main()
	{
	}
}
